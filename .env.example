# WebSocket Server Configuration
WEBSOCKET_HOST=0.0.0.0
WEBSOCKET_PORT=8080

# Riva gRPC Server Configuration
RIVA_HOST=localhost
RIVA_PORT=50051
RIVA_TIMEOUT=30

# Logging Configuration
LOG_LEVEL=INFO

# SSL Configuration (optional)
# If both cert and key are provided, the WebSocket server will start with WSS (TLS)
# WEBSOCKET_SSL_CERT=certs/localhost.crt
# WEBSOCKET_SSL_KEY=certs/localhost.key
# WEBSOCKET_SSL_VERIFY_MODE=none  # valid: none, peer, force_peer (default: none)